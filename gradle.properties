org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=false
android.nonFinalResIds=false

# API versioning (required by root build.gradle)
api_version_major=1
api_version_minor=0
api_version_patch=0

# SDK versions required by the zacharee/ShizukuHackApi submodule (api/api/build.gradle).
# Without these the build fails with:
#   "Could not get unknown property 'api_target_sdk' for extension 'android'"
api_compile_sdk=36
